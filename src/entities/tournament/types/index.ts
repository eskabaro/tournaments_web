export interface ITournament {
    id: string
    title: string
    image: string
    description: string
    game: string
    startDate: string
    endDate: string
    participants: number
    maxParticipants: number
    isJoined: boolean
}
